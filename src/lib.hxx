#ifndef LIB_HXX
#define LIB_HXX

// if you're using module, ignore the export declaration error.

/**
 * @brief Prints out "Hello world"
 *
 * @return nothing but a warm welcome
 */
void say_hello();

#endif // !LIB_HXX
